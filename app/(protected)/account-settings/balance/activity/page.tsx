'use client';

/*
type ActivityPageType = {
  // children: ReactNode;
}
*/

import BalanceAndActivity from '@/components/AccountSettings/Balance/BalanceAndActivity';

export default function ActivityPage(/*{}: ActivityPageType*/) {
  return (
    <BalanceAndActivity />
  );
}
