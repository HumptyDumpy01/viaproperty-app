'use client';

/*
type PendingRequestsPageType = {
  // children: ReactNode;
}
*/

import PendingRequests from '@/components/AccountSettings/Balance/PendingRequests';

export default function PendingRequestsPage(/*{}: PendingRequestsPageType*/) {
  return (
    <PendingRequests />
  );
}
