'use client';

/*
type RejectedRequestsPageType = {
  // children: ReactNode;
}
*/

import RejectedRequests from '@/components/AccountSettings/Balance/RejectedRequests';

export default function RejectedRequestsPage(/*{}: RejectedRequestsPageType*/) {
  return (
    <RejectedRequests />
  );
}
