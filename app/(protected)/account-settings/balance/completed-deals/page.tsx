'use client';

/*
type CompletedDealsPageType = {
  // children: ReactNode;
}
*/

import CompletedDeals from '@/components/AccountSettings/Balance/CompletedDeals';

export default function CompletedDealsPage(/*{}: CompletedDealsPageType*/) {
  return (
    <CompletedDeals />
  );
}
